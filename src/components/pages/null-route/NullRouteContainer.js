import React from 'react';
import RenderLandingPage from './RenderNullRoute';

function NullRouteContainer({ LoadingComponent }) {
  return (
    <>
      <RenderLandingPage />
    </>
  );
}

export default NullRouteContainer;
